spring:
  jpa:
    generate-ddl: true
    open-in-view: false
    show-sql: false
  datasource:
    hikari:
      maximum-pool-size: 200

      
logging:
  level:
    org.sterl.spring.persistent_tasks: debug